let name = 'Hitesh'
   let message = `

      Welcome
      ${name}
      to our world

    `;

  // We can embed variables in backticks and other numbers also
   document.write(message)
   console.log(message);
